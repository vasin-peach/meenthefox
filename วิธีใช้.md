# วิธีใช้ (Meen The Fox)

ไม่ต้องเขียน code — ทำแค่นี้:

## ครั้งแรก (เครื่องใหม่)

```bash
pnpm run setup
```

## ใส่รูปใหม่

1. คัดลอกรูปเข้าโฟลเดอร์หมวดที่ถูก เช่น  
   `media/cars/` · `media/foods/` · `media/behide/` · …
2. **ไม่ต้องเปลี่ยนชื่อเอง** — ระบบจะจัดให้ (เช่น `car-00001.jpg`)

## อัปเดตเว็บ / ส่งขึ้น Git

```bash
git push
```

หรือใช้ `pnpm run push` — ทำงานเหมือนกันแต่ไม่ต้อง push สองรอบ

ก่อน push ระบบจะ **จัดชื่อ + บีบอัดรูป + บันทึกให้อัตโนมัติ + ส่งขึ้น GitHub** ถ้ามีรูปใหม่/เปลี่ยน

หลัง push ไป GitHub แล้ว **Netlify จะดึงโค้ดและอัปเดตเว็บให้เอง** (ต่อ repo กับ Netlify ครั้งเดียวในแดชบอร์ด Netlify)

### ตั้งค่า Netlify (ครั้งเดียว)

1. ใน Netlify เลือก **Add new site → Import an existing project** แล้วเลือก repo นี้จาก GitHub  
2. ค่า build ใช้จากไฟล์ `netlify.toml` ใน repo อยู่แล้ว (โฟลเดอร์เผยแพร่ = `public`)  
3. ได้ URL เว็บจริงแล้ว แก้บรรทัด `og:url` / `og:image` / `twitter:image` ใน `public/index.html` ให้เป็น URL นั้น (สำหรับแชร์ลิงก์ในโซเชียล)

## ชื่อไฟล์หลังระบบจัดให้

| โฟลเดอร์ | ตัวอย่างชื่อ |
|----------|----------------|
| behide | `MTF00170-min.jpg` |
| cinema / family | `MTF00058.jpg` / `DSC03161.jpg` |
| cars, foods, … | `car-00001.jpg`, `food-00001.jpg`, … |
| covers | `cover-00001.jpg` และ `logo.png` |

รายละเอียดสำหรับทีม dev / AI: `.cursor/rules/media-image-optimization.mdc`
